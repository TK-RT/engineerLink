<!DOCTYPE html>
<html>
  <head>
    <title>EngineerLink</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/fbe007337b.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <header class="border-bottom mb-5">
      <nav class="navbar navbar-expand-lg">
        <ul class="nav navbar-nav mr-auto">
          <li class="nav-item m-4">
            <%= image_tag asset_path("logo1.png") %>
          </li>
        </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "マイページ", users_user_path(current_user.id), class: "page-link text-dark d-inline-block btn m-4" %></li>
          <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "page-link text-dark d-inline-block btn m-4" %></li>
        <% else %>
          <%= link_to '新規登録', new_user_registration_path, class: "page-link text-dark d-inline-block btn m-4" %>
          <%= link_to 'ログイン', new_user_session_path, class: "page-link text-dark d-inline-block btn m-4" %>
        <% end %>
      </ul>
    </header>
    
    <%= yield %>
    
  </body>
</html>
